# Flask и Gunicorn для сервера
flask==3.0.3
gunicorn==23.0.0

# Работа с HTTP запросами
requests==2.32.5

# Управление переменными окружения
python-dotenv==1.0.1

# AWS и Elastic Beanstalk CLI (для деплоя, если используешь)
awsebcli==3.20.1
awscli==1.27.7

# Railway CLI и Flyctl через Python не требуются, управление через system shell
